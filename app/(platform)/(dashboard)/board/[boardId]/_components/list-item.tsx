"use client"

import { ListWithCards } from "@/types";

interface ListItemProps {
  data: ListWithCards;
  index: number
}

export const ListItem = () => {
  return (
    <div>
      list item
    </div>
  )
}